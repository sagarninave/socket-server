export enum ENodeEnvironment {
  DEVELOPMENT = "development",
  PRODUCTION = "production",
  TEST = "test",
}

export enum EMongoDbEnvironment {
  DEVELOPMENT = "development",
  PRODUCTION = "production",
  TEST = "test",
}

export enum EAppVersion {
  APP_TYPE = "api",
  VERSION_1 = "v1",
  VERSION_2 = "v2",
  VERSION_3 = "v3",
}
